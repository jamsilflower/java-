package com.ohgiraffers.section3.constant;

public class Application2 {

    public static void main(String[] args) {

     /* 수업목표. 상수의 명명 규칙에 대해 이해할 수 있다. */
     /*필기.
     * 상수의 명명 규칙은 변수의 명명 규칙과 컴파일 에러를 발생시키는
     * 규칙은 동일하다.
     * 하지만 개발자들끼리의 암묵적 규칙에서는 일부 차이가 있다.
     * 1. 모든 문자는 영문자 대문자 혹은 숫자만 사용한다.
     * 2. 단어와 단어 연결은 언더스코어(_)를 사용한다.
      */
     /* 모든문자는 영문자 대문자 혹은 숫자만 사용한다. */
        final int AGE = 20;
        final int AGE2 = 30;


     final  int MAX_AGE = 60; //상수는 합성어를 사용시 언더스코어를 적용. 변수는 대문자로 구분.

       // 어떻게 하는지 모르지만 일단 연습용 테스트 커밋할 수정본
        

    }
}
